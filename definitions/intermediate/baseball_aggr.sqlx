config {
  type: "table",
  schema: "intermediate"
}

SELECT 
  gameId, 
  gameNumber, 
  year, 
  dayNight,
  duration_minutes
FROM 
  ${ref("stg_schedules")}
WHERE 
  dayNight = 'D' 
  and year >= 2016